// Common tap holds
#define PLUS LT(_QWERTY, KC_QUOT) // tap/hold for ´ñ

// Common workspace
#define PRV_WPC LCTL(LGUI(KC_LEFT)) // previous workspace, combo for ctrl+super+left
#define NXT_WPC LCTL(LGUI(KC_RGHT)) // next workspace, combo for ctrl+super+right